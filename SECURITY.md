# 数据保护说明

本公开仓库只发布通用代码、配置模板和脱敏文档，不发布项目专属抗体序列、候选突变库、结构文件、模型输出或实验标签。

项目数据应保存在服务器受控目录中。`.gitignore` 默认拦截常见的 FASTA、PDB/mmCIF、CSV/TSV 和模型权重文件；如确需公开某个数据文件，应先完成去标识化、权属和公开范围审核。

如果历史提交曾包含不应公开的数据，应先从当前工作树删除，再重写 Git 历史并核验远端分支；本仓库在 2026-09-09 已完成一次此类清理。
