[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "evomax-enhanced"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = ["numpy>=1.24"]

[project.optional-dependencies]
esmc = ["transformers>=4.57.6"]

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]
